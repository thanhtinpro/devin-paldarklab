// PALDARK W47 — Save game payload structs (empty TU).
//
// The structs in `PaldarkSaveTypes.h` are pure data; UHT-generated boilerplate
// is sufficient. This TU exists so the .h has a translation-unit pair (the
// validator + the build system expect every Public/Save/*.h to have a
// matching Private/Save/*.cpp).

#include "Save/PaldarkSaveTypes.h"
