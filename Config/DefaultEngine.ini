[/Script/EngineSettings.GameMapsSettings]
GameDefaultMap=/Engine/Maps/Templates/OpenWorld
EditorStartupMap=/Engine/Maps/Templates/OpenWorld
GameInstanceClass=/Script/PaldarkLab.PaldarkGameInstance
GlobalDefaultGameMode=/Script/PaldarkLab.PaldarkGameModeBase
; W26 — L-01 closure. URL-flag fallback for the extraction GameMode.
; Designer can launch with `open Raid_Sandbox?game=Extraction?Experience=PaldarkExperience.PX_RaidSandbox`
; without setting World Override on every map. The matching runtime assertion
; lives in APaldarkGameModeBase::OnExperienceAssetReady (logs Error + on-screen
; red warning if Experience.RequiredGameModeClass is set and the spawned
; GameMode is not a subclass).
+GameModeClassAliases=(Name="Extraction",GameMode="/Script/PaldarkLab.PaldarkGameMode_Extraction")

[/Script/Engine.Engine]
AssetManagerClassName=/Script/PaldarkLab.PaldarkAssetManager
GameViewportClientClassName=/Script/Engine.GameViewportClient

[Core.Log]
LogPaldark=Verbose
LogPaldarkPal=Verbose
LogPaldarkInventory=Verbose
LogPaldarkNet=Verbose
LogPaldarkGAS=Verbose
; W42-43 — AWS backend integration (Cognito login, fleet allocation Lambda).
; Kept distinct from LogPaldarkNet so the noisy HTTP request/response lines
; do not drown out the W14-15 listen-server / W16-17 lag-comp logs that
; designers already grep.
LogPaldarkBackend=Verbose
; W44-45 — Hub UI (Pal Stable + Marketplace controller + UMG widget
; classes). Kept distinct from LogPaldark so the "click → server RPC
; → client callback → widget refresh" trace doesn't drown out the
; W14-15 listen-server / W16-17 lag-comp lines designers grep.
LogPaldarkUI=Verbose

[/Script/Engine.RendererSettings]
r.GenerateMeshDistanceFields=True
r.DynamicGlobalIlluminationMethod=1
r.ReflectionMethod=1
r.Shadow.Virtual.Enable=1

[/Script/HardwareTargeting.HardwareTargetingSettings]
TargetedHardwareClass=Desktop
AppliedTargetedHardwareClass=Desktop
DefaultGraphicsPerformance=Maximum
AppliedDefaultGraphicsPerformance=Maximum

; --- W14-15 Networking ---
; LAN-only OnlineSubsystem so the W14-15 dedicated test can run without Steam
; / EOS / Cognito credentials. Q4 (W42-43) swaps this for the AWS Cognito flow.
[OnlineSubsystem]
DefaultPlatformService=NULL

[/Script/Engine.GameSession]
MaxPlayers=4

; W14-15 dedicated test target: 4 player, 4 Pal, 30 Hz tick on the server.
; NetServerMaxTickRate caps the engine's authoritative simulation rate. The
; per-actor NetUpdateFrequency on AC characters is already tuned in W3-4
; (30Hz player, 15Hz Pal) so this 30Hz cap is consistent.
[/Script/OnlineSubsystemUtils.IpNetDriver]
NetServerMaxTickRate=30
MaxClientRate=15000
MaxInternetClientRate=15000
ServerTravelPause=4
InitialConnectTimeout=120.0
ConnectionTimeout=60.0
